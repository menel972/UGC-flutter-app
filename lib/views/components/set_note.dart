import 'package:flutter/material.dart';

// <> SetNote()
class SetNote extends StatelessWidget {
  // <> Constructor
  const SetNote({Key? key}) : super(key: key);

  // <> Build
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
